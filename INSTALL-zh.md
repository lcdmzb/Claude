# 安装指南（零基础版）

这份指南假设你完全没有编程经验。全程只需要**复制、粘贴、按回车**，大约 5～10 分钟。

---

## 为什么要这一步？

我写的是「源代码」——相当于菜谱。要变成能双击打开的 App，需要在你的 Mac 上「照着菜谱做一次菜」，
这个过程叫**编译**。编译只能在你自己的电脑上完成，所以需要用到「终端」这个自带工具。

编译**只做一次**。做完你就得到一个正常的 App，以后双击图标打开即可，再也不用碰终端。

---

## 第 0 步：确认系统版本

点屏幕左上角的 **苹果图标** → **关于本机**，看 macOS 版本号。

- 版本号 **13 或更大**（Ventura / Sonoma / Sequoia / Tahoe…）→ 可以继续
- 版本号小于 13 → 这个 App 装不了，需要先升级系统

---

## 第 1 步：打开「终端」

1. 按 **Command + 空格**（弹出搜索框）
2. 输入 **终端**
3. 按 **回车**

会打开一个白色或黑色的窗口，里面有一行文字和一个闪烁的光标。**这就是终端。**

> 在终端里，「运行一条命令」= 把文字粘贴进去，然后按回车。
> 粘贴用 **Command + V**，和别处一样。

---

## 第 2 步：安装编译工具

复制下面这行，粘贴到终端，按回车：

```
xcode-select --install
```

**会发生什么：**

- 如果弹出一个窗口问你要不要安装 → 点 **安装** → 点 **同意** → 等它下载完（几分钟到十几分钟，取决于网速）
- 如果终端显示 `command line tools are already installed`（已经装过了）→ 很好，直接进入下一步

> 这是苹果官方的开发工具，免费、安全，装一次就够了。

---

## 第 3 步：下载代码

### 方式 A：网页下载（推荐，更简单）

1. 用浏览器打开这个地址：

   https://github.com/lcdmzb/Claude/tree/claude/macos-sitting-reminder-app-dj0wup

2. 点页面上绿色的 **Code** 按钮
3. 在弹出的菜单里点 **Download ZIP**
4. 下载完成后，在「下载」文件夹里**双击那个 zip 文件**解压
5. 会出现一个文件夹，名字很长，类似 `Claude-claude-macos-sitting-reminder-app-dj0wup`

### 方式 B：用命令下载

如果方式 A 提示你没有权限，就在终端里依次运行这两行：

```
cd ~/Downloads
```

```
git clone -b claude/macos-sitting-reminder-app-dj0wup https://github.com/lcdmzb/Claude.git 站立提醒源码
```

（可能会让你登录 GitHub 账号，按提示操作即可。）

---

## 第 4 步：让终端「进入」那个文件夹

这一步有个小技巧，**不用手打那串长长的文件夹名字**：

1. 在终端里输入 `cd ` —— 注意 **cd 后面有一个空格**，先别按回车
2. 打开**访达**，找到第 3 步解压出来的那个文件夹
3. 用鼠标**把文件夹直接拖进终端窗口**
4. 终端里会自动出现它的完整路径
5. 现在按 **回车**

做完这一步，终端就"站在"项目文件夹里了。

**怎么确认成功？** 运行这行：

```
ls
```

如果屏幕上出现 `build_app.sh`、`Package.swift`、`Sources` 这些名字，说明位置对了。
如果没有，说明拖错文件夹了（可能拖的是外层文件夹，里面还套了一层同名文件夹，进去再试一次）。

---

## 第 5 步：编译并打包

复制这行，粘贴，回车：

```
bash build_app.sh --run
```

**会发生什么：**

- 屏幕上会刷出一堆文字，这是正常的编译过程
- **第一次编译比较慢，1～3 分钟**，请耐心等，不要关窗口
- 中途可能出现黄色的 `warning`（警告）——**警告可以无视**

**成功的样子：** 最后会看到一行

```
✅ 构建完成：/Users/你的用户名/Downloads/.../dist/站立提醒.app
```

然后 App 会自动打开。

**失败的样子：** 出现红色的 `error:` 字样，并且没有 ✅。
这种情况请看本文最后一节「出错了怎么办」。

---

## 第 6 步：首次打开的两个提示

### 提示一：「无法验证开发者」

因为这个 App 不是从 App Store 下载的，macOS 会拦一下。解决办法：

1. 在访达里找到 **站立提醒.app**
2. **右键点击**它（或按住 Control 再点）
3. 选 **打开**
4. 再点一次弹窗里的 **打开**

**只需要做这一次**，以后正常双击就行。

### 提示二：「站立提醒想要发送通知」

点 **允许**。不允许的话，通知中心的提醒会收不到（但全屏提醒和声音还是正常的）。

---

## 第 7 步：把 App 放到「应用程序」里

现在 App 还在下载文件夹里，容易误删。建议：

把 **站立提醒.app** 拖到访达左侧的 **应用程序** 文件夹里。

或者，在终端里运行这行，自动帮你放好：

```
bash build_app.sh --install
```

放好之后，原来的下载文件夹和源码文件夹都可以删掉了（App 是独立的，不依赖源码）。

---

## 第 8 步（可选）：开机自动启动

1. 苹果图标 → **系统设置**
2. 左侧找到 **通用** → **登录项与扩展**
3. 在「登录时打开」里点 **+**
4. 选择 **站立提醒**

这样每次开机它会自动在菜单栏待命。

---

## 怎么用

1. 打开 App，点蓝绿色的 **开始久坐提醒** 按钮 —— 今天就算打卡了，倒计时开始
2. 一小时后会响一段轻柔的琶音，屏幕上弹出全屏提醒
3. 起来活动一下，回来点 **我已起身，完成打卡**（或者等倒计时走完自动完成）
4. 下一轮计时自动开始，循环往复
5. 点上方的 **记录** 可以看日历、连续打卡天数和历史统计
6. **关掉窗口不会停止计时** —— 它会缩到屏幕最上方的菜单栏里继续工作。
   想彻底退出，点菜单栏的图标 → **退出**

---

## 出错了怎么办

编译出错很常见，尤其是第一次。**不用慌，也不会弄坏你的电脑。**

请这样做：

1. 在终端窗口里，按 **Command + A** 全选，再按 **Command + C** 复制
2. 把复制的内容原样发给我
3. 我看过报错就能改好，然后你重新拉一次代码再编译

### 几个常见问题

**问：终端显示 `swift: command not found`**
答：第 2 步的工具没装好。重新运行 `xcode-select --install`，装完再试。

**问：终端显示 `No such file or directory`**
答：第 4 步的文件夹进错了。重做第 4 步，用拖拽的方式。

**问：编译到一半卡住不动了**
答：正常，第一次编译就是慢。等满 5 分钟再说。

**问：App 打开后没有声音**
答：先检查电脑本身没静音；然后在 App 的 **设置** 页面点 **试听** 按钮测试。

**问：我想换成半小时提醒一次**
答：App 里点 **设置** → **提醒间隔** → 选 30 分钟。改完立即生效。

---

## 以后怎么更新到新版本

不用再去网页下载 ZIP。打开终端，粘贴下面这一整行，回车即可：

```
cd ~/Pictures && rm -rf Claude-claude-macos-sitting-reminder-app-dj0wup standup.zip && curl -L -o standup.zip "https://github.com/lcdmzb/Claude/archive/refs/heads/claude/macos-sitting-reminder-app-dj0wup.zip" && ditto -x -k standup.zip . && rm standup.zip && cd Claude-claude-macos-sitting-reminder-app-dj0wup && bash build_app.sh --install --run
```

它会自动完成：删掉旧源码 → 下载最新代码 → 解压 → 重新编译 → 装进「应用程序」→ 打开。

**更新前记得先退出 App**（菜单栏图标 → 退出），否则新版本可能覆盖不上去。

> 这里用的是 macOS 自带的 `ditto` 而不是 `unzip`。
> `unzip` 不认 UTF-8 文件名，遇到中文文件会报 `write error (disk full?)`——
> 那是文件名解析失败的误报，不是磁盘真的满了。

---

## 数据存在哪

你的打卡记录存在这个文件里：

```
~/Library/Application Support/StandUp/store.json
```

在 App 的 **设置** 页面点 **在访达中显示** 就能找到它。换电脑时把这个文件拷过去，记录就跟着走了。

所有数据**只存在你自己的电脑上**，不联网、不上传。
